{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "deploy:site": "lerna exec --scope site -- npm run deploy"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
